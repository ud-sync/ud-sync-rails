require 'spec_helper'

describe Ud::Sync::Rails do
  it 'has a version number' do
    expect(Ud::Sync::Rails::VERSION).not_to be nil
  end

  it 'does something useful' do
    expect(false).to eq(true)
  end
end
